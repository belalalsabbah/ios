name: twonet_customer
description: "2Net Customer App"
publish_to: 'none'

# versionName + versionCode
version: 2.0.2+6

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # =========================
  # 🌐 Network - محدث
  # =========================
  http: ^1.3.0
  dio: ^5.8.0+1
  dartssh2: ^2.8.1
  network_info_plus: 4.1.0+1

  # =========================
  # 🔥 Firebase - محدث
  # =========================
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.4

  # =========================
  # 💾 Storage - محدث
  # =========================
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  # ✅ إضافة للتخزين الآمن (اختياري)
  # flutter_secure_storage: ^9.2.2

  # =========================
  # 📦 App Update / APK - محدث
  # =========================
  open_filex: ^4.7.0
  package_info_plus: ^8.3.0

  # =========================
  # 🔒 Keep screen awake - محدث
  # =========================
  wakelock_plus: ^1.2.10

  # =========================
  # 🧠 State Management
  # =========================
  provider: ^6.1.2

  # =========================
  # 🔗 External links - محدث
  # =========================
  url_launcher: ^6.3.1

  # =========================
  # 🎨 UI
  # =========================
  cupertino_icons: ^1.0.8

  # =========================
  # 📱 Local Notifications - محدث
  # =========================
  flutter_local_notifications: ^18.0.1

  # =========================
  # 🌐 WebView - محدث
  # =========================
  webview_flutter: ^4.10.0

  # =========================
  # 🔄 إعادة تشغيل التطبيق
  # =========================
  restart_app: ^1.3.2

  # =========================
  # ✅ إضافات جديدة مهمة - محدثة
  # =========================
  # للتعامل مع الروابط والـ Deep Linking بشكل أفضل
  # uni_links: ^0.5.1 (معلقة بسبب مشكلة التوافق)
  
  # لمراقبة حالة الاتصال بالإنترنت
  connectivity_plus: ^6.1.3
  
  # للتنسيق والتواريخ
  intl: ^0.20.2
  
  # لعرض رسائل SnackBar محسنة
  another_flushbar: ^1.12.30
  
  # لمعالجة الأخطاء وعرضها بشكل أفضل
  fluttertoast: ^8.2.12
  
  # ✅ للتعامل مع فتح إعدادات التطبيق (مهم لصفحة فحص FCM)
  permission_handler: ^12.0.0+1

  # =========================
  # ✅ إضافات جديدة للمشاركة والواتساب - محدثة
  # =========================
  
  # لمشاركة النصوص والملفات
  share_plus: ^10.1.4
  
  # لإنشاء ومعالجة الصور (اختياري - إذا أردت مشاركة كصورة)
  # image_picker: ^1.0.5
  # screenshot: ^2.1.0
  
  # لعرض شريط تحميل جميل للمشاركة
  # loading_animation_widget: ^1.2.0
  
  # لفتح التطبيقات الخارجية بشكل أفضل
  # android_intent_plus: ^5.0.0

  # =========================
  # ✅ إضافات للتحسين والأداء - محدثة
  # =========================
  
  # للتقليل من حجم الصور وتحسين الأداء
  flutter_native_splash: ^2.4.4
  
  # لأيقونات التطبيق
  flutter_launcher_icons: ^0.14.3
  
  # للتحقق من وجود تحديثات
  # in_app_update: ^4.2.2 (خاص بـ Google Play)
  
  # للتخزين المؤقت للصور - ✅ نسخة واحدة فقط
  cached_network_image: ^3.4.1

  # =========================
  # 📺 IPTV - Xtream Codes - محدث
  # =========================
  
  # مكتبة Xtream Codes API
  xtream_code_client: ^2.0.1
  
  # مشغل فيديو قوي للبث المباشر
  media_kit: ^1.2.0
  media_kit_video: ^1.2.5
  media_kit_native_event_loop: ^1.0.4
  
  # دعم لأنظمة التشغيل المختلفة
  media_kit_libs_android_video: ^1.3.4  # لأندرويد
  media_kit_libs_ios_video: ^1.1.4      # لـ iOS
  media_kit_libs_macos_video: ^1.1.4    # لماك
  media_kit_libs_windows_video: ^1.0.4  # لويندوز
  media_kit_libs_linux: ^1.1.3          # للينكس

  # =========================
  # 🎬 Video Player للأفلام والمسلسلات
  # =========================
  
  # مشغل فيديو للأفلام والمسلسلات
  video_player: ^2.9.2
  
  # واجهة جميلة للفيديو
  chewie: ^1.8.5

  # =========================
  # 🔧 إضافات التحكم بالفيديو - جديدة
  # =========================
  
  # للتحكم في اتجاه الشاشة (عرضي/طولي)
  native_device_orientation: ^2.0.0
  
  # للتحكم في سطوع الشاشة
  screen_brightness: ^2.0.0

  # =========================
  # 🎨 تحسينات الواجهة - إضافات جديدة
  # =========================
  
  # ✅ تم نقل cached_network_image إلى قسم التحسين أعلاه
  
  # تأثير shimmer أثناء التحميل
  shimmer: ^3.0.0
  
  # شبكة متجاوبة للعرض
  flutter_staggered_grid_view: ^0.7.0
  
  # سلايدر للعرض
  carousel_slider: ^5.0.0

  # =========================
  # 🎨 أيقونات إضافية - محدثة
  # =========================
  # أيقونات جميلة للتطبيق
  iconsax: ^0.0.8
  # أيقونات مخصصة
  font_awesome_flutter: ^10.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# =========================
# ✅ إعدادات أيقونة التطبيق
# =========================
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21
  remove_alpha_ios: true
  background_color: "#FFFFFF"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"

# =========================
# ✅ إعدادات شاشة البداية (Splash Screen)
# =========================
flutter_native_splash:
  color: "#2196F3"
  image: "assets/images/splash_logo.png"
  android: true
  ios: true
  
  # أيام الليل (Dark mode)
  color_dark: "#121212"
  image_dark: "assets/images/splash_logo_dark.png"
  
  android_12:
    image: "assets/images/splash_logo.png"
    icon_background_color: "#2196F3"
    image_dark: "assets/images/splash_logo_dark.png"
    icon_background_color_dark: "#121212"

flutter:
  uses-material-design: true
  
  # ✅ إضافة الأصول (Assets)
  assets:
    # أيقونات التطبيق
    - assets/icon/
    - assets/images/
    - assets/icons/
    
  # ✅ إضافة الخطوط (Fonts) إذا لزم الأمر
  # fonts:
  #   - family: Tajawal
  #     fonts:
  #       - asset: assets/fonts/Tajawal-Regular.ttf
  #       - asset: assets/fonts/Tajawal-Bold.ttf
  #         weight: 700